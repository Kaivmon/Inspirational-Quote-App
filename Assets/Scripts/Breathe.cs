using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Breathe : MonoBehaviour {

    // This is just gonna be one of those simply breathing excersises for when you're feeling overwhelmed.
    // an animated circle will expand, slow at first, fast in the middle, and then slow at the end.
    // Same in reverse
    // Text should appear below the circle for the first 3 inhales and exhales, saying breathe in, breathe out.
        // Text should fade in at th estart, fade out at the end.
    // Tap on the circle when you're done to dismiss.



}
